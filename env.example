# ============================================================
# Nest DevTools Telescope - Variáveis de Ambiente
# ============================================================

# API Key para segurança (ALTERE EM PRODUÇÃO!)
DEVTOOLS_API_KEY=changeme-secret-key-minimo-32-caracteres

# Configurações do Banco de Dados (usado apenas quando rodando localmente fora do Docker)
# Se usar Docker, as configurações do docker-compose.yml serão usadas automaticamente
DATABASE_URL=postgresql://devtools:devtools@localhost:5433/nest_devtools

# Redis (usado apenas quando rodando localmente fora do Docker)
REDIS_URL=redis://localhost:6380

# Configurações da API
PORT=4001
NODE_ENV=development

# CORS - URLs permitidas para acessar o backend
CORS_ORIGINS=http://localhost:3001,http://localhost:5173

# Retenção de dados (dias)
RETENTION_DAYS=7

# Rate Limiting
RATE_LIMIT=100

# WebSocket (habilitar para updates em tempo real)
ENABLE_WEBSOCKET=true

# ============================================================
# Configurações para integração com sua app NestJS
# ============================================================

# Quando integrar o agent na sua aplicação, use estas URLs:
# Backend URL: http://localhost:4001
# Frontend URL: http://localhost:3001
